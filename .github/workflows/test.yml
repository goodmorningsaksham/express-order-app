name: Test

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fake-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Fake test pass
        run: echo "Tests passed"
